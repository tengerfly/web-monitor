{
  "name": "@web-monitor/playground",
  "version": "0.0.0",
  "private": true,
  "description": "端到端验证环境：一次性触发性能 / 行为 / 错误 / 回放四类数据，用于本地联调与回归",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview --port 5190",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "test": "node -e \"console.log('no tests yet')\""
  },
  "dependencies": {
    "@web-monitor/behavior": "workspace:*",
    "@web-monitor/core": "workspace:*",
    "@web-monitor/error": "workspace:*",
    "@web-monitor/performance": "workspace:*",
    "@web-monitor/replay": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.6.3",
    "vite": "^6.0.5"
  }
}
