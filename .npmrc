auto-install-peers=true
strict-peer-dependencies=false
link-workspace-packages=true
prefer-workspace-packages=true
resolution-mode=highest
# 关闭 run 前的依赖状态校验，避免其内部再次触发 install
verify-deps-before-run=false
