# 代码评审任务清单 — ⑬-后 后端批次（RealtimeModule）

> 单一事实源。修改方：`Backend-Dev`。裁决口径：「一路到测试」连续模式；P0/P1 全修；P2 记录（CR-B-04/05/06 注记保留）。

| 编号 | 严重度 | 问题（一句话） | 修复判据 | 状态 | 修复说明 |
|---|---|---|---|---|---|
| CR-B-01 | P0 | ResponseInterceptor 二次包装 SSE 帧，event 类型丢失、客户端收不到 snapshot | 帧经拦截器后保留 `event: snapshot` 与裸快照 data | 已确认→已修复 | http.ts 增加 `SSE_RESPONSE_KEY` 元数据跳过（Reflector）+ `SseResponse()` 装饰器打标 stream()；回归断言：透传分支帧原样、plain 分支仍包装 |
| CR-B-02 | P1 | 字符串错误码经过滤器退化为数字（404/50000） | 客户端可按 REALTIME_APP_NOT_FOUND / REALTIME_CLIENT_LIMIT 分支 | 已确认→已修复 | 过滤器对 body 中字符串 code 原样透传（数字 BizCode 行为不变） |
| CR-B-03 | P1 | 503 背压缺 Retry-After 头 | 503 响应携带 Retry-After: 10 | 已确认→已修复 | 异常体声明 retryAfter，过滤器 setHeader('Retry-After') |
| CR-B-04 | P2 | 心跳 15_000 魔法值 | 常量化 | 已确认→已修复 | `HEARTBEAT_INTERVAL_MS` 常量 |
| CR-B-05 | P2 | assertAppExists 每轮一次 Prisma 查询 | — | 保留+注记 | 量级可接受；如需优化并入 TTL 缓存（随 FTDD 实现） |
| CR-B-06 | P2 | SQL 在 Service 层（无 Repository） | — | 保留+注记 | 与存量单文件模块现状基线一致；如引 Repository 层全仓统一 |

## 复核轮次记录
| 轮次 | 范围 | 结果 | 备注 |
|---|---|---|---|
| 第 1 轮 | 后端全量 diff + 三方契约 | P0×1 / P1×2 / P2×3 | 修复完成 |
| 第 2 轮（复核） | 只验 4 条已确认 | ✅ 全部达成：typecheck ✓ lint 0 错 0 警 ✓ vitest 15/15（含 CR-B-01 透传回归断言）✓ nest build ✓（先清过期 tsbuildinfo） | **已放行**：P0=0、P1 清零 |

## 沉淀判定（双向）
| 方向 | 目标 skill | 落点 | 状态 |
|---|---|---|---|
| A · 防复发 | `Backend-Dev` | 错误表 +1 行：全局拦截器 × SSE 帧相互作用须显式打标跳过并以透传断言回归；记其 CONTEXT D-xx | ✅ 已沉淀 |
| B · 检出力 | `Code-Review` | known-issue-patterns +1：「流式/SSE 响应被全局拦截器二次包装」（检出方法：存在全局响应包装拦截器时，逐个流式端点核对帧结构是否被 map 包装） | ✅ 已沉淀 |
