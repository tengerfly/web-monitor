# ⑬-前 前端代码评审（∥ Fidelity-Review）— Realtime 页面（commit d55fb30）

> 通道说明：延续批次惯例，主上下文独立评审；Fidelity-Review（还原度）证据由 §4.7 双开截图流程产生（设计稿 v4 ↔ 实现页并排比对，screenshots/realtime-impl-*.png），问题按约定并入本清单（标来源）。

## 1. 元数据
| 任务 ID | 批次 | 日期 | 结论 |
|---|---|---|---|
| CR-FE-20260918-01 | ⑬-前 | 2026-09-18 | **P0×0 / P1×0 / P2×4**（开发期已修 3 处，评审确认；4 条 P2 注记保留） |

## 2. 静态验证取证
| 项 | 结果 |
|---|---|
| dashboard typecheck | ✅ |
| vitest（dashboard） | ✅ 15/15（hook 状态机 5 + 区块/token 10，TC↔AC 标注齐全） |
| pnpm lint | ✅ 0 errors |
| vite build | ✅（realtime 路由级懒加载；echarts/antd vendor 分包保留） |
| 浏览器双开还原比对 | ✅ 设计稿 v4 ↔ 实现页并排（截图 realtime-impl-02-fixed.png），console 零错误 |

## 3. 问题清单
| 编号 | 来源 | 严重度 | 问题（文件:行） | 成因 | 修复判据 | 状态 |
|---|---|---|---|---|---|---|
| CR-F-01 | Fidelity-Review | P2 | 告警「触发于」时间用浏览器本地时区换算（blocks.tsx formatOccurTime），而统计时点为服务端本地时间切片——两类时间显示口径混用（真实后端 ISO 无 Z 时会自然对齐，mock 为 UTC 时暴露） | 时间格式化未区分「服务端口径切片」与「本地换算」 | 统一：展示时间一律对 generatedAt 同源切片或后端统一下发展示串 | 保留+注记（联调期与后端确认 ISO 是否含时区偏移） |
| CR-F-02 | 前端视角 | P2 | 壳层侧边菜单沿用存量 AntD 浅色样式，与设计稿深色侧栏不一致 | FTDD D4 已声明「框架层继续 AntD」，壳层非本次还原范围 | — | 保留+注记（口径表：壳层=存量样式） |
| CR-F-03 | 前端视角 | P2 | 空态分支 `styles.emptyApp` 同时承担布局与内联 padding 兜底（RealtimeScreen 空应用分支） | 快速实现期的双保险写法 | 可合并为纯 CSS Module | 保留+注记（纯样式细节） |
| CR-F-04 | 前端视角 | P2 | 首帧超时后 SSE 仍保持自动重连 + 兜底轮询并行，重连成功前的重复兜底请求未做去重上限 | D3 双通道设计的边界 | 每 N 轮兜底成功后重建 SSE 一次（FTDD §7.3 已预留语义） | 保留+注记（当前量级无害） |

**开发期已修（评审确认）**：趋势面板双层嵌套（容器多包一层 panel 致标题重复）、HTML 图例与 ECharts 图例双份、告警指标枚举未映射中文展示名——均已在 §4.7 双开比对中捕获并修复，复核截图 realtime-impl-02-fixed.png 确认。

## 4. 契约专项（三方比对，本批收口）
| FTDD fetch | TDD 端点 | 实现 | 结论 |
|---|---|---|---|
| EventSource `…/:appKey/stream` | ✅ | streamUrl() + 原生 EventSource；`snapshot` 具名事件；retry 由服务端帧控制 | ✅ |
| axios `…/:appKey/snapshot` | ✅ | api.ts fetchSnapshot（复用存量 get 封装，拆包语义一致） | ✅ |
| 404 → 应用被删态 | ✅ REALTIME_APP_NOT_FOUND(404) | isAppNotFound 按 response.status===404 | ✅ |
| 字段清单（§5 三类字段） | RealtimeScreenSnapshot | 逐字段消费（metrics/trend/errors[].fingerprint/topLists/alerts 分槽/vitals.null/failure） | ✅ 零缺口 |
| 共享类型单一源 | @web-monitor/types/realtime.ts | 前端仅 import type，零重定义 | ✅ |

## 5. 覆盖边界（强制说明）
已查：fetch↔TDD↔实现三方契约、hook 状态机（单测覆盖 live/interrupted/firstfail/切换关旧连接/兜底轮询）、组件渲染与状态分支、token 对账（34 行→43 变量断言）、还原度双开截图、a11y 基线（语义标签/aria-label/原生 select/焦点可见）。**未查**：真实后端联调（⑭ 覆盖）、ECharts 大数据量渲染性能、axe-core 完整扫描（以语义化+对比度 token 代替）。残余风险：长时挂屏的 ECharts 实例内存（50 点量级风险低）。

## 6. Fidelity-Review 结论（并行方）
设计稿 v4 ↔ 实现页：七区块顺序/结构 ✅；配色 token 逐值一致（#0A0F1E/#101828/#60A5FA/#E8EFFB…）✅；数字 40px tabular-nums ✅；告警横幅/错误流/榜单/四环内容与层级 ✅；console 零错误 ✅。结论：**还原度达标**。
