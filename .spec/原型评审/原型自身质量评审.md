# 原型自身实现质量评审 — web-monitor 实时大屏原型

> 通道：静态源码审查 + 浏览器实测（console 收集器、六态断言、截图）；编号沿用汇总报告 PR-xx。

## 1. 元数据
| 任务 ID | 维度 | 日期 | 结论 |
|---|---|---|---|
| PROTO-REVIEW-20260918-01 | 原型自身实现质量 | 2026-09-18 | P0×0 / P1×0 / P2×4 |

## 2. 不通畅点清单
| 编号 | 出处 | 严重度 | 描述 | 成因 | 修改方向 | 修复判据 |
|---|---|---|---|---|---|---|
| PR-11 | index.html:336 | P2 | dimmed 数组遗漏 topErr/topPage，边缘态下 Top 榜后两列不变暗（截图 05 佐证） | 列数扩充未同步数组 | 数组补全或统一挂 topZone | 三列同步变暗 |
| PR-12 | index.html:282-286 | P2 | a 内嵌 button 属无效 HTML 嵌套（a 不得包含 interactive content） | 修复「查看完整被挤出」时未调外层结构 | 链接区与按钮区双层并列 | 结构合规且行为不变 |
| PR-13 | topPage 定义；趋势生成 | P2 | mock 口径：最差页面用秒（4.2s）与 M01 §5「耗时毫秒」矛盾；趋势仅 24 点 ≠ 60 点 | mock 未对照口径 | 毫秒统一；趋势补 60 点 | mock 与 M01 §5 一致 |
| PR-14 | 告警横幅 vs 指标卡 | P2 | 告警「当前值 2.4%」与指标卡「错误率 0.18%」并存无口径注记；指标卡标红未对应 A11「命中告警阈值才醒目」 | mock 各自拟定 | 统一口径或注明「触发时值」；bad 与阈值命中对齐 | 数据自洽、醒目语义正确 |

## 3. 通过项（浏览器实测 + 静态审查）
- console 零错误（error/unhandledrejection 收集器，交互全量触发后仍为空）；
- 无未定义变量/时序错误（脚本置底、appSelect 先于 setInterval/首次 applyState）；render 全量 innerHTML 覆盖无叠加；2.5s setTimeout < 6s 周期不堆积；
- innerHTML 注入面：数据全部来自本地常量，无外部输入；
- CSS 显隐链与 JS className 操作一一对应（.zfail/.failed-zone/.failbanner.show/.alerts.show/.zonestate.show）；partial→其他态 topZone 强制复位无残留；
- 三栏布局 min-width:0 + ellipsis（Top 榜溢出已在构建期实测修复）；六态 DOM 断言全部通过；三占位页可达且有返回链接。

## 4. 小结
实现质量合格、可运行可验证；4 条 P2 均为 mock 口径与结构合规问题，不阻碍演示。
