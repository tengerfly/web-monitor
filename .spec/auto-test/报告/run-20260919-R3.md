# auto-test 测试报告 — web-monitor（run-20260919）

> 测试方：ZCode auto-test（会话模式，引擎未部署）。执行日期 2026-09-19。
> 基准：PRD V1 AC（对错）/ 设计稿 v4（视觉）/ `packages/types` 契约（接口）。
> 被测对象：实时大屏（`apps/dashboard/src/features/realtime/` + `apps/server` RealtimeModule）及其依赖的确定性门禁全仓。

## 一、运行概览

| 项 | 内容 |
|---|---|
| 项目 / 被测版本 | web-monitor @ 7ce0908（工作区含本轮 4 张任务卡的未提交修复） |
| 运行轮次 | run-20260919-R1（首轮）→ R2（回归）→ R3（回归，终态） |
| 模式 | 会话模式 |
| 测试环境 | mock-realtime-server(8787 新进程) + vite dev(5180 新进程) + In-app Browser（Playwright 面） |
| 分层结果（R3 终态） | L0 Pass → L1 Pass(23/23) → L2 Pass(10/10) → L3 执行(轻量) → L4 未触发（无信号） |

## 二、门禁判定（R3 终态）

| 门禁 | 阈值 | 实测 | 判定 |
|---|---|---|---|
| P0 缺陷 | = 0 | 0 | ✓ |
| 状态覆盖率 | ≥90% | 100%（12/12：E2E 可构造 6 态 + 单测覆盖 6 态，见 用例库/状态机清单.md） | ✓ |
| 迁移覆盖率 | ≥80% | 100%（E2E 可构造迁移 6/6：idle→loading→ok、ok→interrupted、interrupted→live 恢复、ok→切换→ok） | ✓ |
| 元素点击率 | ≥90% | 100%（大屏页可交互元素 8/8：应用切换器、5 条错误链接【href 断言+1 条实点】、查看完整/收起、告警横幅链接【href/target 断言】；壳层全局筛选控件未逐点——数据面在 mock 下为空、动作结果不可判定，归属存量六模块范围） | ✓ |
| 覆盖率 ratchet | 不低于上轮 | 上轮（⑭ 手工验收）14 用例 → 本轮 10 E2E 用例 + 23 契约断言 + 36 单测，全部资产化可重复执行 | ✓ |

**总体判定：PASS**

## 三、缺陷清单与闭环

| 编号 | 级别 | 标题 | 状态 | 任务卡 |
|---|---|---|---|---|
| BUG-20260919-01 | P1 | lint 门禁失败：mock-realtime-server.mjs 6 个 no-undef（阻断 L0） | 已修复(R1)，R3 终验维持 | 任务卡/BUG-20260919-01.md |
| BUG-20260919-02 | P2 | lint warning：useRealtimeStream.test.tsx 未使用 waitFor | 已修复(R1)，R3 终验维持 | 任务卡/BUG-20260919-02.md |
| BUG-20260919-03 | P1 | live 阶段无帧新鲜度看门狗：真实断流不显示失败横幅、恢复不自动重连（半开连接静默冻结） | 已修复(R1)，R3 真实断流复测通过 | 任务卡/BUG-20260919-03.md |
| BUG-20260919-04 | P1 | REST 快照兜底双前缀 404（axios baseURL + snapshotUrl 均含 /api/v1）：断流后误判「应用不可用」并停摆自愈轮询 | 已修复(R2)，R3 恢复复测通过 | 任务卡/BUG-20260919-04.md |

P3 建议（不建卡）：
1. mock 对未知路径返回 `404 {code:0,message:'ok'}`——code 语义不当，建议 `{code:40400,message:'not found'}`（测试基建）；
2. axios 错误拦截器 `message.error(error.response.data.message)` 会把异常 message 原样上屏（本轮表现为误导性「ok」toast），建议对空/非描述性 message 兜底；
3. 本轮修复引入 `FRAME_STALE_TIMEOUT_MS=15s`，若未来调整推送周期（5s）需同步该倍数关系（代码注释已注明）。

## 四、闭环追踪

| 轮次 | 触发原因 | 修复的缺陷 | 新增/回归失败 | 判定 |
|---|---|---|---|---|
| R1 | 首轮全量 | —（发现 BUG-01/02 阻断 L0、BUG-03 于 L2） | 修复后 L0 全绿；L2 断流复测失败（BUG-03） | FAIL → 修复 |
| R2 | 修复 BUG-01/02/03 | BUG-01/02/03 | L0/L1 全绿；断流检测生效，但暴露 BUG-04（双前缀 404 死锁，实测实锤） | FAIL → 修复 |
| R3 | 修复 BUG-04 | BUG-04 | 无。L0（install/typecheck/lint/36 单测/build）+ L1（23/23）+ L2 真实断流→恢复全链路全绿 | **PASS** |

R3 L2 关键复测证据（真实断流注入）：
- 09:31:39 杀 mock → 09:31:53 首采即「更新失败」横幅 + 最后成功统计时点（≤14s，验收线 ≤30s），全程无「该应用不可用」误判（sawNotAvailable=false）；
- 09:32:31 mock 重启就绪 → 09:32:42 首采「正常更新中」（≤11s，验收线 ≤15s），09:33:00 统计时点 01:32→01:33 恢复推进；
- 截图：`evidence/screenshots/realtime_TC07-R3_断流失败横幅无误判.png`；误导性「ok」toast 已消失（代之以真实的 500 提示）。

## 五、L2 用例结果（R3 终态，用例库：用例库/TC-realtime-E2E.md）

| 用例 | 结果 | 证据/说明 |
|---|---|---|
| TC-01 首屏七区块 | ✅ | 默认选中第一应用、七区块齐全、console 0 error；screenshots/realtime_TC01_首屏七区块.png |
| TC-02 SSE 自动推进 | ✅ | 统计时点 00:25→00:26（分钟粒度）自动推进，「正常更新中」 |
| TC-03 应用切换/竞态 | ✅ | 切换重建、快速连续切换以最后选择为准、无残留 |
| TC-04 错误条目跳详情 | ✅ | href=/errors/fp-1 target=_blank；详情路由渲染优雅空态（mock 无该分组） |
| TC-05 告警横幅 | ✅ | 五要素齐全，href=/alerts target=_blank |
| TC-06 超长文本展开 | ✅ | role=note 含完整消息、「查看完整」↔「收起」切换 |
| TC-07 断流失败态 | ✅（R3） | ≤14s 翻转「更新失败」+ 最后成功时点，无误导态 |
| TC-08 恢复自愈 | ✅（R3） | ≤11s 横幅消失 + 时点推进 |
| TC-09 console 卫生 | ✅ | 全程 error/unhandledrejection/console.error 0 记录 |
| TC-10 存量路由遍历 | ✅ | 9 路由全部非白屏有骨架/空态，0 console error |

工具降级声明：环境无 Playwright+Midscene 独立进程，使用会话内 Browser Use（IAB，Playwright API 面）执行；IAB 弹窗策略限制 `target=_blank` 新视图真点（以 href/target 属性断言替代）；SSE 高频重渲染导致 2 次 actionability 超时（改 href 定位/脚本点击兜底，属自动化环境观察项，人工操作正常，不判缺陷）。

## 六、慢接口专项

mock 环境数据瞬时返回（快照 <50ms），无慢接口样本；真实聚合耗时归部署环境按 TDD p95 目标验收（继承上轮边界）。

## 七、L4 专项指标表

| 专项 | 判定 |
|---|---|
| 负载/压力/稳定 | 未触发（无需求信号） |
| 安全扫描 | 未触发（无敏感数据/对外接口信号）；本机依赖审计未跑（归属 CI） |
| 兼容/无障碍 | 未触发（单浏览器 IAB Chromium） |

## 八、覆盖边界声明（强制）

- **已覆盖**：/realtime 七区块渲染与全部页内交互、SSE 生命周期全链路（live→断流检测→失败横幅→恢复自愈，真实进程级断流注入非 mock 模拟）、应用切换/竞态、错误流跳转、告警横幅、超长文本、9 个存量路由冒烟、REST+SSE 契约 23 断言（`packages/types` 逐字段）、全仓单测 36 条（server 15 + dashboard 21，含本轮新增 5 条）、typecheck/lint/build 全量门禁。
- **AI 链路**：不适用（本项目无 LLM 依赖；数据面为 mock-realtime-server 演示数据）。
- **未覆盖**：真实 ClickHouse/PostgreSQL 端到端（本机无 Docker infra，SQL 已由上轮代码评审逐列核对，运行时行为待部署环境验证）；多浏览器/移动端；TV 长时挂屏（PRD Out-of-Scope）；壳层全局筛选控件的动作面（mock 下不可判定）；/alerts 定位交互细节（mock 下空态）。
- **残余风险**：① 生产部署无 vite 代理，SSE 经 nginx/网关的断连与缓冲行为可能与本环境不同，建议部署环境复测断流场景；② 告警「触发于」时区口径（继承上轮待联调确认项）；③ 真实聚合 SQL 的性能（p95）未验。

## 九、证据索引

- L0：`evidence/l0-{install,typecheck,lint,test,build}.log`（R3 为终态）
- L1：`evidence/network/l1-assert-r3.log`（23/23）+ `l1-{projects,snapshot,sse-17s,404-status}.log`
- L2：`evidence/screenshots/`（TC-01 首屏、TC-07-R3 失败态等）+ `evidence/console/`（路由转储）+ 轮询时间线（本文第四节）
- 用例库：`用例库/TC-realtime-E2E.md` + `用例库/状态机清单.md`（可重复执行资产）
- 任务卡：`任务卡/BUG-20260919-0{1,2,3,4}.md`（修复记录 + 回归结论已闭环回填）
