# BUG-20260919-01【P1】lint 门禁失败：mock-realtime-server.mjs 报 6 个 no-undef error

| 字段 | 内容 |
|---|---|
| 级别 | P1（阻断 L0 门禁，`pnpm lint` exit 1） |
| 类型 | 功能（工程门禁） |
| 位置 | `apps/server/mock-realtime-server.mjs`（92/114/115/117/118/131 行） |
| 关联用例 | L0-lint |
| 发现轮次 | run-20260919-R1 |

## 现象
仓库根执行 `pnpm lint`，`apps/server/mock-realtime-server.mjs` 报 6 个 `no-undef` error（`URL`、`setInterval`×2、`clearInterval`×2、`console`），lint 以 exit 1 失败，阻断 L0 确定性门禁。

## 复现步骤（可直接照做）
1. 前置：仓库根 `D:\project\agent-project\web-monitor`，依赖已安装
2. 执行 `pnpm lint`
3. 读输出：`✖ 7 problems (6 errors, 1 warning)`，其中 6 errors 全部来自该文件

## 期望 vs 实际
- 期望：`pnpm lint` 0 errors（exit 0）——基准：仓库 lint 门禁（CI 等价命令）
- 实际：exit 1，6 errors（证据 log 全文见下）

## 证据
- console：`.spec/auto-test/evidence/l0-lint.log`（关键行：`92:19 error 'URL' is not defined no-undef` 等 6 条）
- 定位线索：`eslint.config.mjs` 对 `.mjs` 文件的 `languageOptions.globals` 覆盖情况，以及该文件自加入以来 lint 是否在 CI 门禁内跑过（方向线索，不下结论）

## 回归验收标准（机器判读）
- [ ] 仓库根 `pnpm lint` exit 0，`mock-realtime-server.mjs` 0 error
- [ ] 不放宽其他文件的既有 lint 规则（dashboard/server 源码规则不受影响）

## 修复记录（修复方回填）
| 轮次 | 修复说明（commit） | 回归结论 |
|---|---|---|
| R1 | 修复 `eslint.config.mjs`：既有 globals 覆盖块仅含 `**/*.cjs`/`**/*.config.js`/`build/**/*.mjs`/`**/rollup.config.mjs`，未覆盖 Node 直跑的 `apps/server/mock-realtime-server.mjs`，新增针对该文件的 `languageOptions.globals` 覆盖块（`URL`/`setInterval`/`clearInterval`/`console` 均 readonly），精确限定 files 到该文件，不放宽其他文件既有规则。未改 mock 脚本本身、未升级依赖。自验：`pnpm lint` exit 0（0 problems）；`pnpm --filter @web-monitor/dashboard test` 16/16 全绿 | Pass（R1 全量回归 lint 0 problems；R3 终验维持 0 problems + dashboard 21/21） |
