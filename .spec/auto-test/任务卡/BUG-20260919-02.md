# BUG-20260919-02【P2】lint warning：useRealtimeStream.test.tsx 未使用的 waitFor 导入

| 字段 | 内容 |
|---|---|
| 级别 | P2（warning 不阻断，但项目基准为 0 errors 0 warnings） |
| 类型 | 功能（代码卫生） |
| 位置 | `apps/dashboard/src/features/realtime/hooks/useRealtimeStream.test.tsx`（1 行 27 列） |
| 关联用例 | L0-lint |
| 发现轮次 | run-20260919-R1 |

## 现象
`pnpm lint` 报 `1:27 warning 'waitFor' is defined but never used`。上一轮交付报告（.spec/测试/测试计划与用例结果.md）声明「0 errors 0 warnings」，当前与基准不符。

## 复现步骤（可直接照做）
1. 前置：仓库根，依赖已安装
2. 执行 `pnpm lint`
3. 读输出第 3-4 行：该 warning

## 期望 vs 实际
- 期望：0 errors 0 warnings（基准：上轮交付报告声明的 lint 基线）
- 实际：1 warning（unused import `waitFor`）

## 证据
- console：`.spec/auto-test/evidence/l0-lint.log`
- 定位线索：该测试文件 import 行；若 `waitFor` 确无使用场景则移除导入（方向线索，不下结论）

## 回归验收标准（机器判读）
- [ ] `pnpm lint` 输出 0 problems（errors 与 warnings 均为 0）
- [ ] `pnpm --filter @web-monitor/dashboard test` 仍全绿（删导入不得破坏测试）

## 修复记录（修复方回填）
| 轮次 | 修复说明（commit） | 回归结论 |
|---|---|---|
| R1 | 修复 `apps/dashboard/src/features/realtime/hooks/useRealtimeStream.test.tsx`：`waitFor` 全文件仅出现在 import 行（测试用 `act` + fake timers 实现等待，确无使用），从 `@testing-library/react` 导入中移除 `waitFor`，仅改第 1 行，不动测试逻辑。自验：`pnpm lint` exit 0（0 problems，0 errors 0 warnings）；`pnpm --filter @web-monitor/dashboard test` 16/16 全绿（含该文件 5 例） | Pass（R1 全量回归 lint 0 problems；R3 终验 lint 0 problems，该文件测试随全量 21/21 绿） |
