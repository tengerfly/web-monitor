# 技术方案评审任务清单（⑩-后 批次）— web-monitor 实时大屏 TDD V1

> 单一事实源。修改方：`Backend-Tech-Design`。裁决口径：「一路到测试」连续模式；P1 全修、P2 全修（均为文档级增量）；无「已忽略」。
> 通道说明：本轮子代理凭据不可用，评审由主上下文独立执行（偏差记入埋点自查）。

| 编号 | 严重度 | 问题（一句话） | 修复判据 | 状态 | 修复说明 |
|---|---|---|---|---|---|
| BE-TDD-01 | P1 | 契约 eventId 与 wm_errors 实际列不符（无 event_id，定位标识应为 fingerprint） | 契约/SQL 要点/字段映射三处与 schema 逐列一致 | 已确认→已修复 | 契约改 fingerprint（语义=错误事件定位标识，沿用存量定位方式）；SQL 要点改取 fingerprint/type/message/url AS page/timestamp；§5 字段映射改存量实际列；测试骨架同步 |
| BE-TDD-02 | P2 | 错误码缺 HTTP 状态码、SSE 拒绝形式未定 | 两端点错误行为唯一确定 | 已确认→已修复 | REST=404 / 背压=503+Retry-After；SSE 建流前 404 拒绝 |
| BE-TDD-03 | P2 | 慢接口聚合键「方法」列来源未核 | 聚合键与存量列事实一致 | 已确认→已修复 | 注明 name 列（存量接口统计页口径），方法维度走 extra |
| BE-TDD-04 | P2 | 趋势/最差页面页面路径列名未落 | SQL 要点逐列可对 schema | 已确认→已修复 | pv 页面=name 列、loadTime=value 列（与 overview.avgPageLoad 同款） |
| BE-TDD-05 | P2 | EventSource 重连 Last-Event-ID 语义未声明 | 重连语义唯一确定 | 已确认→已修复 | 声明「重连=取最新快照，不补历史帧」 |
| BE-TDD-06 | P2 | 权限「沿用存量校验」与存量实际无鉴权存在落差 | 现状与取舍披露 | 已确认→已修复 | 总纲 §1 现状披露：存量无 Guard，本模块保持一致不新增 |

## 复核轮次记录
| 轮次 | 范围 | 结果 | 备注 |
|---|---|---|---|
| 第 1 轮 | TDD 全量（⑩-后 批次） | P0×0 / P1×1 / P2×5 | 修复完成 |
| 第 2 轮（复核） | 只验 6 条修复判据 | ✅ 全部达成（eventId/fingerprint 终验、404/Last-Event-ID/现状披露落点确认） | **已放行**：P0=0、P1 清零 |

## 沉淀判定（双向）
| 方向 | 目标 skill | 落点 | 状态 |
|---|---|---|---|
| A · 防复发 | `Backend-Tech-Design` | 错误表加行：契约字段与存储列名须逐列 grep schema 核对，禁凭记忆写列 | ✅ 已沉淀（CONTEXT D-xx 记录） |
| B · 检出力 | `Tech-Review` | known-issue-patterns +1：「契约字段凭记忆写、与存储列不符」（检出方法：契约/SQL 要点逐列 grep 建表脚本） | ✅ 已沉淀 |
