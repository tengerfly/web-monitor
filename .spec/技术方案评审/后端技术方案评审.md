# ⑩-后 后端技术方案评审 — web-monitor 实时大屏 TDD V1

> 通道说明：子代理派发因平台凭据暂不可用，本评审由主上下文独立执行（对照 backend-checklist BE-T1~T8 + 已知模式 1~3 逐条过筛；通道偏差记入埋点自查）。基线：PRD V1（已放行）+ 存量代码事实（clickhouse-schema.sql / errors.module.ts / app.module.ts）。

## 1. 元数据
| 任务 ID | 批次 | 日期 | 结论 |
|---|---|---|---|
| TDD-REVIEW-20260918-01 | ⑩-后（仅 TDD） | 2026-09-18 | **P0×0 / P1×1 / P2×5** |

## 2. 需求承接对照表
| PRD REQ / AC | TDD 承接 | 结论 |
|---|---|---|
| REQ-M01-001 核心指标（AC-001） | overview §7 载荷 metrics 七项 + 模块 §4 指标口径 | ✅ |
| REQ-M01-002 趋势（AC-002） | trend 60 点补空 + §7 AC-M01-002 | ✅ |
| REQ-M01-003 错误流（AC-003） | errors ≤50 倒序含定位标识 | ⚠️ 定位标识字段名与存储事实不符（BE-TDD-01） |
| REQ-M01-004/013 Top 榜（AC-004/013） | 三榜单各≤5 + 聚合键 | ✅（聚合键方法列来源见 BE-TDD-04） |
| REQ-M01-005/012 告警（AC-005/012） | unresolved≤100 + recent24h 含 status | ✅ |
| REQ-M01-006/014 达标率（AC-006/014） | 三项+overall，无样本 null | ✅ |
| REQ-M01-007/008/011 持续更新/失败标记（AC-007/008/011） | failure.status + lastGood + partial 置空 | ✅ |
| REQ-M01-010 应用无效（AC-010） | REALTIME_APP_NOT_FOUND | ✅（状态码见 BE-TDD-03） |
| REQ-M01-015 并发隔离（AC-015） | TTL 缓存共享（spy 断言 6 非 12） | ✅ |
| 数据统计时点（全部 AC） | generatedAt + statsWindow | ✅ |

## 3. 问题清单
| 编号 | 出处 | 严重度 | 现象（引用原文） | 成因（闭环类型） | 修改方向 | 修复判据 |
|---|---|---|---|---|---|---|
| BE-TDD-01 | 01 模块 §4 契约「errors: Array<{ eventId, … }>」+「含 event_id」；§5「wm_errors(event_id, …)」 | **P1**（数据流断链） | 存量 `clickhouse-schema.sql` 的 `wm_errors` **没有 event_id 列**（实际列：fingerprint/type/message/url/…）；且存量错误详情定位方式=fingerprint（errors.module.ts 按 fingerprint 归组与查询）。契约字段凭记忆写成 event_id，开发期必然返工 | 数据流：契约字段产出侧（SQL）无来源 | 契约字段改 `fingerprint`（业务名「错误事件标识」语义不变，SUM-04「沿用存量错误详情定位方式」）；§5 字段映射与 SQL 要点同步改；「所属页面」列名落为 `url` | 契约/SQL 要点/字段映射三处与 schema 逐列一致 |
| BE-TDD-02 | overview §7 错误响应 | P2 | REALTIME_APP_NOT_FOUND 未写 HTTP 状态码；SSE 握手拒绝形式未定 | 接口要素不全 | REST=404；SSE 建连前 404 拒绝（不建立流） | 两端点错误行为唯一确定 |
| BE-TDD-03 | 01 §4 Top 榜 SQL 要点 | P2 | 「接口路径+方法」的方法列在 wm_events 无独立列（可能在 extra JSON） | 聚合键落点未核 schema | 注明「分组键沿用存量接口统计页口径（name 列）；方法维度如有需求从 extra 提取」 | 聚合键与存量列事实一致 |
| BE-TDD-04 | 01 §4 趋势/最差页面 | P2 | 「页面路径」列未落具体列名（pv 事件页面路径在 `name` 列） | 同上 | 写明 pv 页面=name 列、loadTime=value 列（与 overview avgPageLoad 同款） | SQL 要点逐列可对 schema |
| BE-TDD-05 | overview §7 SSE | P2 | EventSource 重连 Last-Event-ID 语义未声明 | 快照模型下无需补发，但未显式声明 | 声明「重连=取最新快照，不按 Last-Event-ID 补帧」 | 重连语义唯一确定 |
| BE-TDD-06 | overview §1 安全合规 vs 存量事实 | P2 | PRD 写「沿用存量应用级可见范围校验」，存量代码实际**无任何鉴权中间件**（app.module 无 Guard）；TDD 顺着 PRD 写「沿用」存在歧义 | PRD 假设与代码事实的落差未披露 | 显式声明「存量现状=无应用级校验，本模块保持一致（内网工具、不新增权限模型）」 | 现状与取舍披露，开发期零歧义 |

## 4. 逐维检查记录（BE-T1~T8）
| 维度 | 结论 | 备注 |
|---|---|---|
| BE-T1 需求承接 | ✅（1 处字段名瑕疵见 BE-TDD-01） | 7 REQ / 11 AC 全承接，见对照表 |
| BE-T2 数据模型 | ⚠️ | 无新表论证成立；消费列映射与 schema 不符处=BE-TDD-01/03/04 |
| BE-T3 状态机 | ✅ | failure.status 三态流转触发完备；背压 503 有进有出 |
| BE-T4 流程 | ✅ | SSE 生命周期图与时序图闭环、心跳/退避/清理有终态 |
| BE-T5 接口设计 | ✅（P2×2） | 四补齐全；BE-TDD-02/05 |
| BE-T6 边界与关联 | ✅ | 关联图+表双边一致；应做/不应做明确 |
| BE-T7 异常与边界 | ✅ | 并发（缓存共享）、幂等（只读）、超限（50/100/5、200 连接）、空数据（零值/空）、恢复（lastGood）；Anti-Happy-Path ≥3 失败场景/功能点 |
| BE-T8 越界 | ✅ | 未写前端实现细节 |

已知模式对照：模式 1（总文档同步）本次一次成文、已 grep 核对 ✅；模式 2（预算分层）类目超时 3s < TTL 5s ≤ 推送 5s < 心跳 15s，层间数值有序、击穿责任方（首帧回退 lastGood）已写 ✅；模式 3（双向同时缺失）⑩-前 收口，本批次不适用。

## 5. 小结
**部分能→修复后能**：架构与流程闭环成立、可开工度高；唯一 P1 是契约字段与存储列事实不符（修正为 fingerprint 后即可编码）。
